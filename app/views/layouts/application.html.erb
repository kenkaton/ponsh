<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ponsh" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Google Maps API -->
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_MAPS_API_KEY'] %>&libraries=places"></script>

    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="search">
    <%= render 'shared/header' %>
    <div id="search-container" class="fixed z-10 w-full bg-white shadow-md top-16 hidden" data-search-target="container">
      <div class="container mx-auto px-5 py-4">
        <%= render partial: 'shared/search_form', locals: { visible: '', query: params[:query] } %>
      </div>
    </div>

    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>

    <footer class="mt-12 py-6 bg-gray-100">
      <div class="container mx-auto px-5">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="mb-4 md:mb-0">
            <p class="text-gray-600">&copy; <%= Date.today.year %> Ponsh. All rights reserved.</p>
          </div>
          <div class="flex space-x-6">
            <%= link_to "利用規約", page_path("terms"), class: "text-gray-600 hover:text-gray-900" %>
            <%= link_to "プライバシーポリシー", page_path("privacy"), class: "text-gray-600 hover:text-gray-900" %>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
