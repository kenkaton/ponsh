<!DOCTYPE html>
<html class="h-full">
  <head>
    <%= render_gtm_head %>
    <title><%= content_for(:title) || "Ponsh" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-prefetch" content="true">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Google Maps API -->
    <% api_key = Rails.application.credentials.dig(:google, :maps_api_key) || ENV['GOOGLE_MAPS_API_KEY'] %>
    <% if api_key.present? %>
      <script src="https://maps.googleapis.com/maps/api/js?key=<%= api_key %>&libraries=places" onerror="console.error('Google Maps APIの読み込みに失敗しました')"></script>
    <% else %>
      <!-- Google Maps APIキーが設定されていません -->
      <script>
        console.warn('GOOGLE_MAPS_API_KEYが設定されていません。地図機能を利用するにはAPIキーを設定してください。');
      </script>
    <% end %>

    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="search gtm" class="flex flex-col min-h-screen" data-action="turbo:load->gtm#pageView">
    <%= render_gtm_body %>
    <%= render 'shared/header' %>
    <div id="search-container" class="fixed z-10 w-full bg-white shadow-md top-16 hidden" data-search-target="container">
      <div class="container mx-auto px-5 py-4">
        <%= render partial: 'shared/search_form', locals: { visible: '', query: params[:query] } %>
      </div>
    </div>

    <main class="container mx-auto mt-28 px-5 flex flex-grow pb-12">
      <%= yield %>
    </main>

    <footer class="mt-auto py-6 bg-gray-100">
      <div class="container mx-auto px-5">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4 md:gap-0">
          <div>
            <p class="text-gray-600">&copy; <%= Date.today.year %> Ponsh. All rights reserved.</p>
          </div>
          <div class="flex space-x-6">
            <%= link_to "利用規約", page_path("terms"), class: "text-gray-600 hover:text-gray-900" %>
            <%= link_to "プライバシーポリシー", page_path("privacy"), class: "text-gray-600 hover:text-gray-900" %>
          </div>
          <div>
            <div class="text-sm text-gray-600">
              <a href="https://developers.rakuten.co.jp/" target="_blank" rel="noopener noreferrer">
                Supported by Rakuten Developers
              </a>
            </div>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
